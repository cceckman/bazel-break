load("//:rule.bzl", "my_little_rule")

platform(
    name = "baremetal",
    constraint_values = ["@platforms//os:none"],
)

my_little_rule(
    name = "some-rule",
    target_compatible_with = [":baremetal"],
)
